<div class="padded padded--underheader huge_bottom">
	{% for block in section.blocks %}
	{% assign collection = collections[block.settings.collection] %}

	<div class="huge_bottom">{% include 'collection' %}</div>
	{% endfor %}
</div>


{% schema %}
{
  "name": "Collections",
  "settings": [
  ],
  "blocks": [
    {
      "type": "collection",
      "name": "Collection",
      "settings": [
		{
			"id": "collection",
			"type": "collection",
			"label": "Collection"
		}
      ]
    }
  ]
}
{% endschema %}