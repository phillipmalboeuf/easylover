{% if product.compare_at_price > product.price %}
<span class="highlight">{{product.price|divided_by:100}} CAD</span>
<span class="strike">{{product.compare_at_price|divided_by:100}} CAD</span>
{% else %}
{{product.price|divided_by:100}} CAD
{% endif %}